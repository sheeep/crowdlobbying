{% extends 'base.html.twig' %}

{% block title %}{{ campaign }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>Thanks....</h1>
            </div>
        </div>
        <div class="row">
            <div class="col">
                <div class="colored-entry" style="background-color: {{ campaignEntry.color }};">
                    <div class="meta">
                        <p>Von: {{ campaignEntry.person }}</p>
                        <p>An: {{ campaignEntry.politician }} ({{ campaignEntry.politician.party.short }}/{{ campaignEntry.politician.region.short }})</p>
                        <p>Betreff: {{ campaignEntry.campaign }}</p>
                    </div>
                    <p class="argument">
                        {{ campaignEntry.argument }}
                    </p>
                </div>
            </div>
        </div>
        <div class="row share-row">
            <div class="col text-center">
                <a href="{{ path('app_campaign_statement', {'campaign': campaignEntry.campaign, 'id': campaignEntry.id}) }}" class="btn btn-primary">
                    <i class="fab fa-facebook-f"></i>
                </a>
            </div>
            <div class="col text-center">
                <a href="{{ path('app_campaign_statement', {'campaign': campaignEntry.campaign, 'id': campaignEntry.id}) }}" class="btn btn-info">
                    <i class="fab fa-twitter"></i>
                </a>
            </div>
            <div class="col text-center">
                <a href="{{ path('app_campaign_statement', {'campaign': campaignEntry.campaign, 'id': campaignEntry.id}) }}" class="btn btn-success">
                    <i class="fab fa-whatsapp"></i>
                </a>
            </div>
            <div class="col text-center">
                <a href="{{ path('app_campaign_statement', {'campaign': campaignEntry.campaign, 'id': campaignEntry.id}) }}" class="btn btn-warning">
                    <i class="fas fa-envelope"></i>
                </a>
            </div>
        </div>
        <div class="row">
            <div class="col">
                <p>Bla bla bla, noch jemanden anschreiben? in deinem kanton gäbe es noch ...</p>
            </div>
        </div>
        <div class="row">
            <div class="col">
                <script src="https://donorbox.org/widget.js" paypalExpress="false"></script><iframe src="https://donorbox.org/embed/crowdlobbying-digitaler-pass" height="685px" width="100%" style="max-width:500px; min-width:310px; max-height:none!important" seamless="seamless" name="donorbox" frameborder="0" scrolling="no" allowpaymentrequest></iframe>
            </div>
        </div>
    </div>
{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css" integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf" crossorigin="anonymous">
    <style>
        .colored-entry {
            padding: 15px;
            color: #ffffff;
        }
        
        .share-row {
            margin: 25px 0;
        }
        
        .share-row .btn {
            cursor: pointer;
        }
    </style>
{% endblock %}

{% block javascripts %}
{% endblock %}
